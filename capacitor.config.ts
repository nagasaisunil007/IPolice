import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trafficguardian.app',
  appName: 'TrafficGuardian',
  webDir: 'dist/public',
  bundledWebRuntime: false
};

export default config;
